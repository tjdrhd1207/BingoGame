/* import { router } from '../utility/router.js'

export function joinGame(element) {
    console.log(element);
    element.innerHTML = `Join This Game`
    
    const clickBtn = () => {
        router('/bingo');
    }
    element.addEventListener("click", clickBtn);
} */